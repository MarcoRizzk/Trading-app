# Comma-separated Capitol Trades politician IDs to sync daily
POLITICIAN_IDS=P000197

# Cron schedule (default: 06:00 daily). Set ENABLE_SYNC_CRON=false to disable in-process cron.
SYNC_CRON_SCHEDULE=0 6 * * *
ENABLE_SYNC_CRON=true
